# Codex Git Button Test

This is a small test project for checking whether the Codex app enables Git,
diff, terminal, and editor buttons after the folder is initialized as a Git
repository.

commit button test
这是一句测试用语
